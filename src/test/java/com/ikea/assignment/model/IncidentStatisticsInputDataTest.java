/*
package com.ing.assignment.model;

import org.junit.Test;

import java.time.LocalDateTime;

import static org.junit.Assert.assertEquals;

public class IncidentStatisticsInputDataTest {
    @Test
    public void testInputData() {
        IncidentStatisticsInputData inputData = IncidentStatisticsInputData.builder()
                .assetName("CRM")
                .startTime(LocalDateTime.of(2019, 04, 01, 19, 23, 54))
                .endTime(LocalDateTime.of(2019, 04, 01, 19, 25, 23))
                .severity(1).build();

        assertEquals("CRM", inputData.getAssetName());
        assertEquals(LocalDateTime.of(2019, 04, 01, 19, 23, 54), inputData.getStartTime());
        assertEquals(LocalDateTime.of(2019, 04, 01, 19, 25, 23), inputData.getEndTime());
        assertEquals(1, inputData.getSeverity());
    }
}
*/
