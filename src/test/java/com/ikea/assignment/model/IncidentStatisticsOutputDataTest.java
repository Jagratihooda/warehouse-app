/*
package com.ing.assignment.model;

import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class IncidentStatisticsOutputDataTest {
    @Test
    public void testOutputData() {
        IncidentStatisticsOutputData outputData = IncidentStatisticsOutputData.builder()
                .assetName("CRM")
                .totalDownTime(5678)
                .totalIncidents(1)
                .rating(10).build();

        assertEquals("CRM", outputData.getAssetName());
        assertEquals(5678, outputData.getTotalDownTime());
        assertEquals(1, outputData.getTotalIncidents());
        assertEquals(10, outputData.getRating());
}
}
*/
